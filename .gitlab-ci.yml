pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -a index.html app.js styles.css manifest.json sw.js icon-192.png icon-512.png fonts vendor public/
  artifacts:
    paths:
      - public
